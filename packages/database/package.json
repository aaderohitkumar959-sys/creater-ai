{
    "name": "@creator-ai/database",
    "version": "1.0.0",
    "main": "index.ts",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prisma": "prisma",
        "postinstall": "prisma generate --schema=prisma/schema.prisma",
        "build": "prisma generate --schema=prisma/schema.prisma"
    },
    "prisma": {
        "seed": "ts-node seed.ts"
    },
    "devDependencies": {
        "@types/node": "^20.19.25",
        "prisma": "^5.0.0",
        "ts-node": "^10.9.1",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@prisma/client": "^5.0.0"
    }
}